  <body>
        <h1>Irwin Fuentes</h1>

        <table>
        <thead>
                <tr>
                        <th>Project Name</th>
                        <th>Description</th>
                        <th>URL</th>
                </tr>
        </thead>
        <tbody>
 <% PROJECTS.each do |project, data| %>
            <tr>
                <td><%= project %></td>
                <td><%= data['description'] %></td>
                <td><%= link_to data['url'], data['url'] %></a></td>
            </tr>
        <% end %>



        </tbody>
        </table>

